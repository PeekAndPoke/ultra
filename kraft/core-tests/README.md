# Kraft Core Tests

Integration tests for the `kraft-core` module. This is an internal test module and is not published as a library.

## What it covers

- Component rendering and lifecycle
- Reactive state updates and re-rendering
- Routing, navigation, and parameter handling
- Form binding and validation
- Data loading and async streams

## Running the tests

```bash
./gradlew :core-tests:jsTest
```
